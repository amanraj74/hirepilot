{
  "name": "@devfusion/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo 'shared: lint pending'",
    "typecheck": "echo 'shared: typecheck pending'",
    "test": "echo 'shared: test pending'",
    "build": "echo 'shared: build pending'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
