# DevFusion — docs

Authoritative artifacts live at the repo root (`AGENT.md`, `PROJECT_STATUS.md`, `TODO.md`, `README.md`, `CHANGELOG.md`).

This folder will hold:

- `adr/` — Architecture Decision Records
- `architecture/` — diagrams (C4 model, sequence diagrams)
- `postmortems/` — incident retrospectives
- `runbooks/` — operational playbooks

Nothing here yet.
